const express = require('express');
const app = express();

// 1. یہ آپ کی اپنی بنائی ہوئی API Key ہے
const MY_OWN_API_KEY = "Umar_Video_Auth_9988"; 
app.get('/', (req, res)
app.use(express.json());

// 2. یہ وہ راستہ ہے جہاں Antigravity ڈیٹا بھیجے گا
app.post('/make-video', (req, res) => {
    
    // یوزر کی بھیجی ہوئی کی (Key) کو پکڑنا
    const providedKey = req.headers['x-api-key'];

    // 3. میتھمیٹیکل لاجک (Check if Key matches)
    if (providedKey === MY_OWN_API_KEY) {
        
        // اگر کی ٹھیک ہے تو یہاں ویڈیو کا کام شروع ہوگا
        const videoText = req.body.text; 
        console.log("کمپیوٹر ویڈیو بنا رہا ہے: " + videoText);

        res.status(200).json({
            success: true,
            message: "آپ کی کی درست ہے! ویڈیو پروسیسنگ شروع کر دی گئی ہے۔",
            logo: "Zenith-Logo-Added"
        });

    } else {
        // اگر کی غلط ہے تو یہ ایرر بھیجے گا
        res.status(403).json({
            success: false,
            message: "غلط API Key! آپ یہ ٹول استعمال نہیں کر سکتے۔"
        });
    }
});

app.listen(3000, () => console.log("آپ کا سسٹم پورٹ 3000 پر تیار ہے"));
